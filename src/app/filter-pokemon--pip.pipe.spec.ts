import { FilterPokemonPipPipe } from './filter-pokemon--pip.pipe';

describe('FilterPokemonPipPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPokemonPipPipe();
    expect(pipe).toBeTruthy();
  });
});
